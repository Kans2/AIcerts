# AIcerts
# Run locally

1. Install:
   npm install

2. Start dev server:
   npm run dev

3. Visit:
   http://localhost:3000

Notes:
- Versions are saved to data/versions.json (create the folder if missing).
- To reset history, stop server and clear data/versions.json to [].
